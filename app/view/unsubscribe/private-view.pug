extends ../layout/private

block title
  title 配信停止の詳細｜技術ブログ管理システム

block breadcrumb
  nav.mt-3.mb-3(aria-label='パンくずリスト')
    .container
      ol.breadcrumb.mb-0
        li.breadcrumb-item
          a(href='../../') ホーム
        li.breadcrumb-item
          a(href='../') 配信停止
        li.breadcrumb-item
          a(href='./' aria-current='page') 詳細

block main
  main
    .container
      h1 配信停止の詳細
      nav.mt-3(aria-label='メニュー')
        .d-flex.flex-wrap.gap-2
          a.btn.btn-outline-secondary(href='../') 戻る
          a.btn.btn-outline-danger(href='./delete/') 削除...
      section
        h2.mt-3 目次
        nav(aria-label='目次')
          ul.list-unstyled
            li
              a(href='#about') 配信停止について
      section
        h2.pt-3.mt-3.border-top(id='about') 配信停止について
        dl.mb-0
          dt 日時
          dd #{unsubscribe.dateText}

          dt メールアドレス
          dd #{unsubscribe.email}
