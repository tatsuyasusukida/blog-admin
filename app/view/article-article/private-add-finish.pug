extends ../layout/private

block title
  title 記事紐付けの登録完了｜技術ブログ管理システム

block breadcrumb
  nav.mt-3.mb-3(aria-label='パンくずリスト')
    .container
      ol.breadcrumb.mb-0
        li.breadcrumb-item
          a(href='../../../') ホーム
        li.breadcrumb-item
          a(href='../../') 記事紐付け
        li.breadcrumb-item
          a(href='../') 登録
        li.breadcrumb-item
          a(href='./' aria-current='page') 完了

block main
  main
    .container
      h1 記事紐付けの登録完了
      nav.mt-3(aria-label='メイン')
        .d-grid
          a.btn.btn-success(href=`../../${layout.req.query.id}/`) 記事紐付けを表示する
